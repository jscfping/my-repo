


```
git init
(u=jscfping && git config user.name "$u" && git config user.email "$u@gmail.com")
git lfs install
echo "* -text" > .gitattributes

git worktree list
git worktree add ../feat1 -b feat1
git worktree remove -f ../feat1
```






